
import React from 'react';

const Heading = ({title}) => {
    return (
        <div>
            <h1 className='text-center text-3xl font-bold'>{title}</h1>
        </div>
    );
};

export default Heading;